#include <stdio.h>
int main(){
    int n,a;
    scanf("%d %d",&n,&a);
    if(if n==a){
        printf("Equal");
    }
    else if (n>a  ){printf("First");}
    else if (a>n ){printf("Second");}
    
    else{
        printf("No");
    }
    }

