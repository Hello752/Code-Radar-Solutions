#include <stdio.h>
int main(){
    int n,a;
    scanf("%d %d",&n,&a);
    if (n>a  ){printf("First");}
    else if(if n==a){
        printf("Equal");
    }
    else if (a>n ){printf("Second");}
    
    else{
        printf("No");
    }
    }

