#include <stdio.h>

int main() 
{
    char c;
    
    // Prompt the user to enter a year
    scanf("%c", &ch);
    
    // Check if the year is a leap year
    if(isupper(ch)){
        printf("Uppercase",ch);
    }
    else (islower(ch)){
        printf("Lowercase",ch);
    }
    
    return 0;
    

}
