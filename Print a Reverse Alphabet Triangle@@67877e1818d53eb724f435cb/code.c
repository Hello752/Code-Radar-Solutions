#include<stdio.h>
int main(){
   char N,col,count=1;
   scanf("%c",&num);
   for(row=1;row<=N;row++){
    for(col=1;col<=row;col++)
    {printf("%d ",count++);}
   
    printf("\n");}
   return 0;
}