#include<stdio.h>
int main(){
   int num,row,col;
   scanf("%d",&num);
   for(col=1;col<=row;col++){
    for(row=1;row<=num;row++)
    printf("%d ",col);

   
   printf("\n");}
   return 0;
}